import { TextCommand } from ".";

export const command: TextCommand = {
	name: "points",
	description: "View the top 8 users who have been given the most points in the current channel. Add --g to the end to see a points report for the entire server.",
	execute: () => { },
};
